# Copy this file to application-local.yaml and add only local-only values.
# application-local.yaml is gitignored and loaded by the local Spring profile.

spring:
  ai:
    openai:
      api-key: __missing__
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: __missing__
            client-secret: __missing__

vocavista:
  media:
    veo:
      api-key: __missing__
