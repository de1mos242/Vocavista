# Copy this file to application-local.yaml and add only local-only values.
# application-local.yaml is gitignored and loaded by the local Spring profile.

spring:
  ai:
    openai:
      api-key: __missing__

vocavista:
  media:
    elevenlabs:
      api-key: __missing__
